import { ShortenContentPipe } from './shorten-content.pipe';

describe('ShortenContentPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenContentPipe();
    expect(pipe).toBeTruthy();
  });
});
